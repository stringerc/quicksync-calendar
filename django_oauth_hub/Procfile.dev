source .env

# Application
web: gunicorn oauth_hub.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 30

# Database migration (run once)
release: python manage.py collectstatic --noinput && python manage.py migrate